[phases.setup]
nixPkgs = ["python311", "gcc", "g++", "make"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install --with-deps chromium"
]

[start]
cmd = "python main.py"
